# version: "3.9"

services:
  app:
    image: kraken-trading-bot:0.0.2  # Use the pre-built image
    container_name: kraken-trading-bot
    command: python main.py  # Replace with your app's entry point
